<div class="mt-4 mb-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2><i class="bi bi-collection me-2"></i>Administrare grupuri</h2>
        <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#newGroupModal">
            <i class="bi bi-plus-lg me-1"></i>Grup nou
        </button>
    </div>
    <%- include('../partials/flash') %>
        <div class="card">
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead>
                            <tr>
                                <th>Nume</th>
                                <th>Ordine</th>
                                <th>Statistici</th>
                                <th class="text-end">Acțiuni</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% groups.forEach(g=> { %>
                                <tr>
                                    <td class="fw-bold">
                                        <%= g.name %>
                                    </td>
                                    <td>
                                        <%= g.displayOrder %>
                                    </td>
                                    <td><span class="badge bg-primary">
                                            <%= g._count.assignments %> buc
                                        </span></td>
                                    <td class="text-end">
                                        <button class="btn btn-sm btn-outline-primary edit-group-btn me-1"
                                            data-id="<%= g.id %>" data-name="<%= g.name %>"
                                            data-order="<%= g.displayOrder %>"><i class="bi bi-pencil"></i></button>
                                        <form action="/<%= orgId %>/admin/groups/<%= g.id %>/delete" method="POST"
                                            class="d-inline"
                                            onsubmit="return confirm('Sigur doriți să ștergeți: <%= g.name %>?')">
                                            <button type="submit" class="btn btn-sm btn-outline-danger"><i
                                                    class="bi bi-trash"></i></button>
                                        </form>
                                    </td>
                                </tr>
                                <% }); %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
</div>
<div class="modal fade" id="newGroupModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <form action="/<%= orgId %>/admin/groups" method="POST">
                <div class="modal-header">
                    <h5 class="modal-title">Grup nou</h5><button type="button" class="btn-close"
                        data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3"><label class="form-label">Nume</label><input type="text" class="form-control"
                            name="name" required></div>
                    <div class="mb-3"><label class="form-label">Ordine afișare</label><input type="number"
                            class="form-control" name="display_order" value="0"></div>
                </div>
                <div class="modal-footer"><button type="button" class="btn btn-secondary"
                        data-bs-dismiss="modal">Închide</button><button type="submit"
                        class="btn btn-primary">Creare</button></div>
            </form>
        </div>
    </div>
</div>
<div class="modal fade" id="editGroupModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <form id="editGroupForm" method="POST">
                <div class="modal-header">
                    <h5 class="modal-title">Editare grup</h5><button type="button" class="btn-close"
                        data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3"><label class="form-label">Nume</label><input type="text" class="form-control"
                            name="name" id="editGroupName" required></div>
                    <div class="mb-3"><label class="form-label">Ordine afișare</label><input type="number"
                            class="form-control" name="display_order" id="editGroupOrder"></div>
                </div>
                <div class="modal-footer"><button type="button" class="btn btn-secondary"
                        data-bs-dismiss="modal">Închide</button><button type="submit"
                        class="btn btn-primary">Salvare</button></div>
            </form>
        </div>
    </div>
</div>
<script>
    document.querySelectorAll('.edit-group-btn').forEach(btn => {
        btn.addEventListener('click', () => {
            document.getElementById('editGroupForm').action = '/<%= orgId %>/admin/groups/' + btn.dataset.id + '/edit';
            document.getElementById('editGroupName').value = btn.dataset.name;
            document.getElementById('editGroupOrder').value = btn.dataset.order;
            new bootstrap.Modal(document.getElementById('editGroupModal')).show();
        });
    });
</script>