<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="/">
            <i class="bi bi-graph-up-arrow me-2"></i>MAKH
        </a>

        <% if (typeof user !=='undefined' && user) { %>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/<%= user.organizationId %>/statistics">
                            <i class="bi bi-grid-3x3-gap me-1"></i>Grafikonok
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/<%= user.organizationId %>/statistics/overlay">
                            <i class="bi bi-layers me-1"></i>Átfedés
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/<%= user.organizationId %>/statistics/summary">
                            <i class="bi bi-clipboard-data me-1"></i>Összefoglalás
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/<%= user.organizationId %>/statistics/conditions">
                            <i class="bi bi-speedometer2 me-1"></i>Állapotok
                        </a>
                    </li>

                    <% if (user.role==='admin' ) { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                <i class="bi bi-gear me-1"></i>Admin
                            </a>
                            <ul class="dropdown-menu dropdown-menu-dark">
                                <li><a class="dropdown-item" href="/<%= user.organizationId %>/admin/users">
                                        <i class="bi bi-people me-2"></i>Felhasználók
                                    </a></li>
                                <li><a class="dropdown-item" href="/<%= user.organizationId %>/admin/groups">
                                        <i class="bi bi-collection me-2"></i>Csoportok
                                    </a></li>
                                <li><a class="dropdown-item" href="/<%= user.organizationId %>/admin/audit">
                                        <i class="bi bi-journal-text me-2"></i>Audit Napló
                                    </a></li>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li><a class="dropdown-item" href="/<%= user.organizationId %>/admin/settings">
                                        <i class="bi bi-building me-2"></i>Szervezet
                                    </a></li>
                                <li><a class="dropdown-item" href="/<%= user.organizationId %>/statistics/export-all">
                                        <i class="bi bi-download me-2"></i>Összes exportálása (CSV)
                                    </a></li>
                            </ul>
                        </li>
                        <% } %>
                </ul>

                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-person-circle me-1"></i>
                            <%= user.name %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-dark">
                            <li><span class="dropdown-item-text text-muted small">
                                    <%= user.organization ? user.organization.name : '' %>
                                </span></li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li><a class="dropdown-item" href="/logout"><i
                                        class="bi bi-box-arrow-right me-2"></i>Kijelentkezés</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
            <% } %>
    </div>
</nav>